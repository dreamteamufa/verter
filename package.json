{
  "name": "verter",
  "version": "1.0.0",
  "description": "PCS pipeline for Verter SAFE trading bot",
  "main": "src/Verter.user.js",
  "type": "commonjs",
  "scripts": {
    "syntax": "node scripts/syntax-check.js src/Verter.user.js",
    "lint": "eslint src/Verter.user.js --quiet",
    "test": "node --test",
    "build": "node scripts/make-zip.js",
    "pcs": "npm run syntax && npm run lint && npm run test && npm run build"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "esprima": "^4.0.1",
    "archiver": "^6.0.0"
  }
}
